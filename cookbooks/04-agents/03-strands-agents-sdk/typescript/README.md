# TypeScript — not yet ported

The TypeScript implementation of this recipe does not exist yet. Python is the reference
implementation; ports land beside it.

When it does, it will live here as `strandsAgentsSdk.ts` with its own `package.json` and
`tsconfig.json`. The loop is the same shape; the tool functions become async and the
transcript is an array of response items exactly as in Python.

The recipe's narrative lives once in [`cookbooks/04-agents/01-the-agent-loop/README.md`](../README.md) and applies to both
languages.
