# TypeScript — not yet ported

The TypeScript implementation of this recipe does not exist yet. Python is the reference
implementation; ports land beside it.

When it does, it will live here as `toolCalling.ts` with its own `package.json` and
`tsconfig.json`. The tool schema is the same flat shape in the Node SDK, and `call_id`
pairing is identical.

The recipe's narrative lives once in [`cookbooks/02-reasoning-and-output/03-tool-calling/README.md`](../README.md) and applies to both
languages.
